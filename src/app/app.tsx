import React from 'react';
import AppBar from '../components/AppBar';
import RouterConfig from './router';

const App = () => {
  return (
    <div>
      <AppBar />
      <RouterConfig />
    </div>
  );
};

export default App;